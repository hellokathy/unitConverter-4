<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.bmeskill.unitConverter"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <!--         android:debuggable="true" -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_unit_convert_v2"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".LandingPage"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DistanceActivity"  android:label="@string/app_name"></activity> 
    	<activity android:name=".MassActivity"  android:label="@string/app_name"></activity> 
    	<activity android:name=".SpeedActivity"  android:label="@string/app_name"></activity>
    	<activity android:name=".FuelActivity"  android:label="@string/app_name"></activity>
    	<activity android:name=".CurrencyActivity"  android:label="@string/app_name"></activity>
    </application>

</manifest>